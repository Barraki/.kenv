export type DisplayObject = {
  raw: string;           // Raw display path
  manufacturer: string;  // Manufacturer code (e.g., "DEL")
  model: string;        // Model number
  uid: string;          // UID number
  guid: string;         // GUID
};
